# WAKARU v0.1.0 品質報告

検査日: 2026-08-19（公開に向けた独立監査。それ以前の節は2026-08-06時点、初版2026-08-04）
検査環境: Apple Silicon Mac / macOS / ローカルプレリリースビルド

## v0.1.0: 公開に向けた独立監査（2026-08-19）

バージョン番号を内部の開発通し番号 `0.99.0` から、公開ソフトウェアのsemver慣例に合わせて `0.1.0` に付け直した。あわせて、メモリ安全性・アプリの安定性・UI仕様準拠を独立して再監査した。実機での指摘を起点にした従来の各節（v0.99.0時点、下記）とは異なり、**今回は内部監査（コードレビュー）が起点。** 見つけた不具合はすべて修正し、再検査を通した。

### フロントエンド — 見つけて直した不具合

| 不具合 | 再現条件 | 原因 | 対応 |
|---|---|---|---|
| Live Illustratorが古い応答で新しい解説を上書き | 低速なAI接続でページを連続送り、先のページの応答が後から届く | 応答の採否が「停止したか」だけを見ており、「今表示すべき応答か」を照合していなかった | 要求ごとにidを持たせ、`onMutate`で発行・`onSuccess`で現在idと一致する場合のみ反映 |
| 引用を連続クリックすると別の資料が開く | 1件目の`openTab`完了前に2件目をクリック | 開く処理に順序保証がなく、後着優先の競合状態 | 世代カウンタを持たせ、最新の要求のみ`setActiveId`を実行 |
| 資料の見え方の変更が保存前に画面を離れると消える | スライダー操作直後（250ms以内）に画面を離れる | デバウンス保存がアンマウントで単に破棄されていた。表示はキャッシュ済みで変更「済み」に見えるが、実体は未保存 | アンマウント時に保留中の保存を必ず実行。保存失敗時のトースト通知も追加 |
| 未捕捉の例外で画面全体が白紙になる | 資料やAI生成内容の描画中に例外 | トップレベルのエラーバウンダリが存在しなかった | `ErrorBoundary`を追加し、再読み込みで復帰できる画面を表示 |

### Rustバックエンド — 見つけて直した不具合

| 不具合 | 再現条件 | 原因 | 対応 |
|---|---|---|---|
| 極端な縦横比のPDFページでアプリ全体が強制終了 | MediaBoxの縦横比が極端なPDFページを開く | 描画バッファのサイズがページのMediaBoxからそのまま計算され、上限がなかった。Rustのアロケータは確保失敗時にプロセス全体を中断する（`Result`で捕捉できない） | 描画前にピクセル総数へ上限（6,400万px）を設け、超過時は通常のエラーとして扱う |
| ZIPプロジェクト取り込みで宣言サイズを実サイズが超過できる | ヘッダの申告サイズは小さいが実際の展開後サイズが大きいエントリ | サイズ上限の検査がZIPヘッダの申告値のみに基づいていた | 実際に`io::copy`で読み取ったバイト数そのものを上限で区切るよう変更 |
| 資料取り込み中の異常終了でジョブが「実行中」のまま固まる | 取り込み処理の途中で内部エラー（パニック）が発生 | 完了処理（`jobs.finish`）が正常終了の経路でしか呼ばれていなかった | `Drop`で完了処理を保証するガード（`JobFinishGuard`）を導入。異常終了時も必ず「失敗」で完了する |
| テキスト／画像取り込みにサイズ上限がなかった | 巨大なテキスト・コードファイル、または細工された画像ヘッダの取り込み | Office XML（64MB）やZIP取り込みには上限があったが、この経路にはなかった | テキスト系に64MBの上限、画像デコードに明示的な寸法上限（image crateの既定512MB確保上限に加えて）を追加 |
| 想定外のMCPサーバー種別で`unreachable!()`によるプロセス停止の余地 | `mcp_servers`テーブルを手動編集し、`transport`に未対応の値を入れる | 通常の操作では届かないが、ユーザーが直接編集できるSQLiteファイルが入力元だった | 通常のエラーとして返すよう変更（ハードニング。実運用での再現報告ではない） |

いずれも実機での不具合報告を起点にしたものではなく、公開前の内部監査で見つけたもの。修正後、以下はすべて合格：

```
cargo clippy --all-targets --all-features -- -D warnings   # 0 warnings
cargo test                                                  # 67 passed, 0 failed, 3 ignored
npm run typecheck / lint / test                             # 0 errors, 20/20 tests passed
npm run check:tokens / check:i18n / check:contrast          # すべて合格
```

### 監査で確認し、問題なしと判断したもの（抜粋）

- `unsafe`ブロック（PDFKit FFI、sqlite-vec拡張登録）: ライフタイム・シグネチャとも健全
- APIキーの扱い（不変条件I-4）: keyringのみに保存され、DB/JSONへ平文で残らないことを確認
- 原本ファイルの不可侵性（不変条件I-3）: `sources/`配下は取り込み時の1回書き込み以外、削除以外で変更されない
- サンドボックス脱出（不変条件I-7）: `write_file`/`run_command`は自動許可時も`sandbox::resolve_in_sandbox`を必ず経由
- SQL: 文字列組み立てなし、全箇所パラメータ化
- Mutex/RwLockの中毒（poisoning）: 全箇所が`Err`を握り潰さず処理し、ロック区間も最小限

## 判定

実機での指摘（入力カーソルが1文字で切れる、UIの配置が分かりにくい、ボタンの形と動作が分かりにくい）を起点に、フロントエンドを再監査しました。指摘はいずれも再現し、原因を特定して修正しています。あわせて意匠を作り直し、同種の不具合を自動検査で落とせるようにしました。

**このリリースで検証していない範囲は「未検証」と明記します。前回の報告にあった記述のうち、今回のUI改修後に再確認していないものは下の節へ移しました。**

## 前回報告の訂正

前回の品質報告は「アプリ全体をアドホック署名し、`codesign --verify --deep --strict` 合格」と記載していましたが、**DMGの中身を直接検証したところ不合格でした**（`code has no resources but signature indicates they must be present`）。署名識別子を指定せずにビルドすると、Tauriが生成識別子でアドホック署名し、封の整合しない成果物を作るためです。今回は `APPLE_SIGNING_IDENTITY="-"` を明示してビルドし、**DMGをマウントしてその中のアプリを検証**して合格を確認しています（D-13）。

## 2回目の指摘の再現と対応

| 指摘 | 実測した現状 | 対応 |
|---|---|---|
| Live解説が資料に被る | `position: absolute` + modal z-index で資料の上に描画 | Viewerを2カラム化。非モーダルの補助パネルへ |
| Studioが左に寄りすぎ | ペインにパディングが無く空状態がx=0に接地。さらにStudio表示中も資料パネルが残存 | 3カラム構成へ作り直し。資料パネルはViewerペイン専用に |
| 設定がわかりにくい | 14セクションが1本のスクロール。分類・案内なし | 左カテゴリレール+右パネル（10カテゴリ）+設定内検索 |
| 幅を広げると中央に固まる | 1680px幅で左右592px（35%）が空白 | ページ枠の中央固定を撤去。実測で余白0 |
| 縁取りが過剰 | Button 56個中32個が枠線付き | 既定を枠線なしの淡い塗りへ。実測で枠線付きボタン0個 |
| Markdownが出ない | AI出力3箇所すべて素のテキスト描画 | react-markdown + remark-gfm。生HTMLは無効のまま |
| プロジェクト管理 | 全操作が「選択してから」の一括のみ | 表形式＋行ごとの操作。一括は選択時のみ表示 |

## 1回目の指摘の再現と原因

| 指摘 | 再現 | 原因 |
|---|---|---|
| 入力カーソルが1文字で切れる | 再現（`value="a"` / `activeElement=BUTTON`） | `Dialog` のフォーカス用 `useEffect` が `onClose` を依存配列に持ち、呼び出し側のインライン関数の同一性が毎描画で変わるため、1打鍵ごとに effect が破棄・再実行され、フォーカスが閉じるボタンへ移っていた |
| ボタンの形が分かりにくい | 再現 | `--radius-pill` `--color-accent-ink` `--font-reading` `--reading-measure` `--dur-reduced` の5トークンが未定義のまま参照され、ブラウザが宣言ごと破棄していた。ピル型が角丸0の四角に、アクセント上の文字色が継承色のままになっていた |
| 主ボタンと通常ボタンの区別がつかない | 再現 | どちらも枠線のみで、塗りの差がなかった |
| ペインタブと資料タブが混同する | 再現 | 仕様は「セグメンテッドコントロール、下線なし」と規定していたが、実装は両方とも下線式だった |
| 資料の追加が見つけにくい | 再現 | 常に閉じた `<details>` の中にあった |
| 書体が指定と違う | 再現 | 指定した3書体をいずれも同梱しておらず、全画面がシステム標準へ落ちていた |

## 合格した自動検査

- TypeScript型検査、ESLint（警告0）、Vitest 18件、axeアクセシビリティ検査（設定10カテゴリすべてを個別に検査）
- 本番用フロントエンドビルド、デザイントークン規則、3言語キー整合
- Rust整形、Clippy（全ターゲット・全機能、警告をエラー扱い）、通常テスト59件（無視3件）
- `check:contrast`（新規）: `tokens.css` のOKLCH値から実描画される25組の配色を再計算し、ライト・ダークの両方で本文4.5:1・境界3:1を満たすこと、全トークンがsRGB色域に収まることを確認
- `check:tokens`（拡張）: 未定義トークン参照・`position: fixed`・書体の直書き・生の色値・直値z-index・`outline: none` を検出

## 実描画で確認した項目

ビルド済みのフロントエンドを実ブラウザで描画し、計算後のスタイルとDOMを直接検証しました。

- 入力欄に23文字を連続入力し、全文字が入りフォーカスが入力欄に残ることを確認（指摘の症状が解消）
- ダイアログがスクロール領域の外側、アプリ直下のオーバーレイ層へ描画されることをDOM上で確認
- 同梱書体が実際に読み込まれること（`document.fonts.check` で Geist・JetBrains Mono が `loaded`）
- 主ボタンがアクセント塗り＋`--color-accent-ink` の文字色、角丸6px、高さ37.5pxで描画されること
- 無効ボタンが `opacity: 0.5` かつ `pointer-events: auto`（説明ツールチップを出せる状態）であること
- コマンドパレットが Cmd+K で開き、候補の絞り込みと選択位置の表示が動くこと
- ライト／ダーク／モノトーン、表示倍率100%／150%の各組み合わせ
- モノトーン（`filter: grayscale`）適用下でもオーバーレイが正しい位置に出ること
- 最小ウィンドウ相当の960×640で横スクロールが発生しないこと（`scrollWidth === clientWidth`）
- 960 / 1440 / 1680px の各幅で、ホーム・設定・検索・ファイル変換の4画面に横スクロールが無く、ページ幅がウィンドウ幅と一致すること（余白0）を実測
- 設定の全10カテゴリがレールから到達でき、それぞれaxe違反0であることをテストで固定
- Studioの再構築を回帰テストで固定（Markdownが見出し・表・リスト・コードとして描画されること、会話一覧に各行の操作があること、会話作成後にカーソルが入力欄へ入ること、作業領域が出ること）

## 3回目の指摘（2026-08-05）の検証

実機のユーザーから11件の指摘。全件に対応し、**今回はビルドしたアプリのウィンドウ上で手で操作して確認しました**（前回まで未実施だった項目）。内訳は [`../docs/10_改修記録と設計.md`](../docs/10_改修記録と設計.md) §7bis。

| 確認したこと | 方法 | 結果 |
|---|---|---|
| PDFのコントロールドックが消えたこと | 3ページのPDFを取り込み、実機ウィンドウで表示 | ドックなし。ページはアプリが描いた画像として表示 |
| ページ送りとページ数 | 「次へ」でページ2へ | ツールバーが `1 / 3ページ` → `2 / 3ページ`。総ページ数が取れている |
| 資料下部の本文テキスト | 同上 | 表示されない |
| 資料の見え方（白黒反転・露出低減） | 設定でオンにし、ペイン切替の横の操作から適用 | 資料だけが反転・減光。周囲の面はそのまま。アプリ再起動後も保持 |
| 「資料を見る」の横に出る操作 | 設定の表示スイッチを4件オン | オンにしたものだけが出る |
| サイドバーの設定一覧と取り込み | 実機で開いて確認 | 全カテゴリへの導線と「ZIPからインポート」。書き出しは無い |
| プロジェクト管理の移動 | 設定→データ | 一覧・ZIP保存・取り込み・アーカイブ・削除がここにある。ホームからは消えた |
| Studioの下部UI | 会話を新規作成 | 入力欄・参照範囲・送信がすべて画面内。横スクロールなし。空の会話に文言は出ない |
| コマンドパレットの枠 | ⌘K | 細い仕切り線。スクリムと影で分離 |
| ZIP取り込み | 書き出したZIPを実機で取り込み | 成功。検証後に同じ画面から削除して元の状態へ戻した |

**この確認で見つけて直したもの**（実機でしか出なかった2件）:

- 資料の見え方の `filter` を資料の面ごとに掛けていたため、反転すると**周囲の面まで明るくなっていた**。フィルタを資料そのものに限定
- Studioの作業領域ヘッダーが列幅を超え、パス表示がペインの外へはみ出していた。両端を省略表示に

## 4回目の指摘（2026-08-06）の検証

12件。**すべて実機のウィンドウ上で操作して確認しました。**

| 確認したこと | 結果 |
|---|---|
| サイドバーが「設定」1行のみになったこと | 最下部に固定。カテゴリの一覧は消えた |
| 横3本線ボタンの位置 | バーの端から余白が入った |
| 詳しさの切替の既定 | 新規の設定では表示しない（既存の保存済み設定はそのまま） |
| ↓キーでページ送り | 1→2→3ページ。検索欄にキャレットがあるときは進まない |
| 詳しさをオフにしたときのパネル | 「標準 再生成」の帯そのものが出ない |
| 再生成の位置 | 解説の下。生成中は同じ位置が「停止」 |
| 質問欄 | 質問の下に「参照範囲」と「質問する」が1行。余白が詰まった |
| 「URLから追加」 | 押すとURL欄だけが開く |
| 資料を見るのホーム | 1行30pxのリスト。同じ欄で資料内検索、行末で削除 |
| 「ホーム」タブの位置 | 内容と同じ位置から始まる |
| ボタンの字 | 本文の1.2倍・ダークでは白 |
| 全画面 | ⌃⌘Fで全画面へ入る。ページがペインいっぱいに表示される |

**安定性**: 全画面の出入りを4回連続、ウィンドウのドラッグリサイズ、資料を開いたままのペイン切り替えを行い、プロセスは落ちませんでした。`~/Library/Logs/DiagnosticReports` にWAKARUのクラッシュ報告はありません。**「全画面でクラッシュ」は再現していません**（再現したのは ⌃⌘F をアプリが横取りしていた問題で、これは修正済みです）。

**この確認で見つけて直したもの**:

- 「検索」ボタンで検索欄を開いてもキャレットが入らず、続けて打った文字が入力されないうえ ↓ がページ送りとして働いた → ⌘F と同じようにフォーカスを移す

## アスペクト比の検証（2026-08-06）

指摘「1920×1200だとUIが下で途切れて空白ができる」を受けて、**ビューポート5通り × 主要5画面**で `#app` の最下端とビューポート下端の差を実測しました。

| ビューポート | ホーム | プロジェクト | 設定 | 検索 | ファイル変換 |
|---|---|---|---|---|---|
| 1920×1200 | 0 | 0 | 0 | 0 | 0 |
| 1920×600 | 0 | 0 | 0※ | 0 | 0※ |
| 900×1400 | 0 | 0 | 0 | 0 | 0 |
| 2560×1080 | 0 | 0 | 0 | 0 | 0 |
| 960×640（最小ウィンドウ） | 0 | 0 | 0 | 0 | 0※ |

※ スクロール領域の中身がビューポートより長い状態（スクロールで到達できる）。横方向のはみ出しは全条件で0。

**CSSのレイアウトは縦横比に依存していません。** 残る原因はWebViewがウィンドウに追従しそこねる経路で、これは `keep_webview_glued_to_window`（Rust側、`Resized`・`ScaleFactorChanged` でずれているときだけ合わせ直す）で対処しました。**この最後の修正を入れたビルドの実機確認は、途中で画面がロックされたため未了です。**

## 5回目の指摘（2026-08-06 夕方）の検証

| 確認したこと | 結果 |
|---|---|
| 文字の大きさ | 本文15px、下限13px（従来は本文13px・下限11px）。実機で確認 |
| Live Illustratorの太字 | パネル全体が `font-weight: 600` |
| 見えにくいボタン | `ghost`／`danger` にも塗りが付き、実機で全ボタンが面として見えることを確認 |
| 1920×1200の下部の空白 | **真因を特定**（下記）。修正後、ペインが最下端まで満ちることを実測（`paneBottomGap: 0`） |
| ウィンドウ追従 | 1920×1200 / 1280×1024 / 1280×520 で計測、いずれも隙間0・横はみ出し0。全画面の出入りも確認 |
| 中央配置 | ホーム・検索・ファイル変換・資料なしのViewerが、ウィンドウが余っているときは中央、足りないときは従来通りスクロール |

**下部の空白の真因**: `.viewerPane` が `auto minmax(0,1fr)` の2行グリッドで、1行目は**音声・動画があるときだけ描画される**パネルの席でした。音声も動画も無いプロジェクトではその要素が存在せず、Viewerが1行目に入って内容の高さに縮み、残りが空きます。前回「CSSはビューポートを満たしている」と報告しましたが、**測っていたのは `#app` の最下端まで**で、ペイン内部を辿っていませんでした。今回は内部の高さの鎖を上から測って特定しています。

## 6回目の指摘（2026-08-10）の検証

実機のユーザーから2件の指摘。1件目は再現して原因を特定・修正、2件目はUIの指摘を起点に**Studioの中核機能が仕様どおり実装されていないこと**を発見し、実装しました。

| 指摘 | 再現・原因 | 対応 |
|---|---|---|
| 「資料を追加できませんでした」のポップアップが時間が経っても消えない | 再現。`ProjectPage` のエラー表示が `useMutation` の `error` 状態にそのまま連動した常設のバナーで、消えるための仕組み（タイマーや明示的な閉じる操作）が無かった。`design.md` 自身が「トーストは失敗時にのみ発火する」と規定しているのに反していた | `useToast` の失敗トースト（既定5秒で自動的に消える、既にプロジェクト管理などで使われている経路）に置き換え。常設バナーの `<p role="alert">` は削除 |
| Studioタブの見出し「Studioタブ」と「新しいタブ」ボタンの並びを消し、タブ一覧の下に `+` ボタンを置きたい | UI要望どおり | ラベル行を削除し、`aria-label` はペイン自体に残したまま、タブ一覧の下に `+` の追加ボタンを設置。回帰テストでラベル行が消えたこと・`+` が一覧の下にあることを固定 |

### Studioの「予定していた動作」の精査

「PDFなどの文書作成やコード作成、アプリ内フォルダでのコマンド実行」について、`StudioService::send` の実装を読み、**製品仕様（`docs/01_製品仕様.md` FR-T5・FR-T6）が要求する動作が実装されていないことを確認しました**。

- `messages` テーブルには `tool_calls` / `status` 列が、`Settings` には `sandbox.command_timeout_sec` / `sandbox.auto_allow_new_file_writes` が、`sandbox_run_command` コマンドにはサンドボックス実行そのものが、**すでに存在していました**。しかし `StudioService::send` はこれらを一切呼ばず、`Studioへの依頼文に「Markdown」などの語を含む場合に限り、AIの回答をそのまま `workspace/summary.md` へ書く` という固定処理だけを行っていました。ファイル名・内容はAIの回答文そのもの、書き込み対象は常に `summary.md` 固定で、コマンド実行は一切呼び出されていませんでした。
- フロントエンドの承認カードも同様に、実際にAIが何を求めたかによらず `{"tool": "write_file", "path": "summary.md"}` という固定のJSONを表示していました。

**実装した内容**:

- `AiService` にOpenAI互換の `tools` / `tool_choice` を使う `complete_with_tools` を追加（既存の `complete` はリトライ・認証まわりのロジックを共有するリファクタのみで、外部からの挙動は変えていません）。
- `StudioService::send` は `write_file`（workspace内への任意のファイル作成）と `run_command`（`sandbox::run_command` によるworkspace内でのサンドボックス実行）の2つのツールをモデルに提示し、モデルが実際に呼んだツールだけを承認待ちメッセージ（`status: "pending_approval"`）として保存するようにしました。根拠となる資料が見つからない場合でも、ファイル作成・コマンド実行の依頼自体は妨げません（回答の根拠づけとは別の要求のため）。
- 新規コマンド `studio_resolve_tool` で「許可」「拒否」を受け、許可時のみ `sandbox::resolve_in_sandbox` / `sandbox::run_command` を実際に呼び出します。設定の「書き込みを自動許可」がオンの場合、`write_file` のみ承認を待たずに実行します（`run_command` は常に承認が必要）。
- フロントエンドの承認カードは、モデルが実際に提案したツール名・引数を表示するように変更しました。`run_command` は実行するコマンド行をモノスペースで強調表示します（`docs/06_UI仕様.md` §5.3の規定どおり）。承認待ちの間は新しいメッセージの送信を止めます。

**検証**: ローカルのモックHTTPサーバーを使い、`cargo test` で以下を確認しています（実LLMエンドポイントは使っていません。実機でのAI接続確認は他のAI依存機能と同様、下記「検証していないこと」を参照）。

- ツール呼び出しの提案は即座に実行されず、承認するまでworkspaceにファイルが作られないこと
- 承認するとファイルが実際に書かれ、内容が一致し、成果物として記録され、タブを閉じても残ること
- 拒否するとファイルが作られないこと
- 承認待ちが無い状態で解決しようとするとエラーになること（古い・存在しない提案を実行してしまわないこと）
- 「書き込みを自動許可」設定がオンのとき、承認なしでファイルが書かれること
- `run_command` がサンドボックス内で実際にプログラムを実行し、標準出力が回答メッセージに含まれること（ネストしたサンドボックスが使えない検査環境では自動的に読み飛ばします。`sandbox.rs` の既存テストと同じ扱いです）

フロントエンドは `Vitest` で、承認待ちメッセージがインラインの承認カードとして表示されること・承認待ちの間は送信ボタンが無効になること・「許可」を押すと実際に解決APIが呼ばれることを確認しています。`npm run typecheck` / `npm run lint` / `npm run test` / `npm run check:i18n` / `npm run check:tokens` / `npm run check:contrast` / `npm run build`、`cargo test` / `cargo clippy -- -D warnings` はいずれもこの変更を含めて合格しています。

**見つけて直した副次的な不具合**: Studioの送信が、初回表示時（タブを一度もクリックする前）は空文字列のタブIDへ向けて送信されようとしていました。`activeId` の初期値が空文字列のまま使われており、表示上は最初のタブが選択されて見えるのに、実際の送信先はどのタブにも一致しないIDでした。表示に使っている解決済みのタブIDを送信・解決の両方で使うように統一しています。

## 配布物検証

- アプリ: `WAKARU.app`、arm64、バージョン0.99.0、最小macOS 11.0
- 同梱書体6ファイル（latin / latin-ext、正体のみ、計196KB）がアプリのアセットとして埋め込まれていることを確認
- DMG: 19,624,745 bytes（約18.7 MiB）、`hdiutil verify` 合格。SHA-256は `RELEASE_CHECKSUMS.txt` を正とします
- DMG内容: `WAKARU.app` と `Applications` リンクを読み取り専用マウントで確認
- **コード署名: DMG内のアプリを直接検証し、`codesign --verify --deep --strict` 合格。** 署名はアドホック、識別子は `com.yukiorita.wakaru`
- Gatekeeper: Developer ID未署名のため `spctl` は想定どおり拒否。一般配布には署名・公証が必要です
- ビルドしたアプリを起動し、`WAKARU initialized version="0.99.0"` のログ出力と、異常終了しないこと・正常に終了することを確認

## 今回のリリースで検証していないこと

前回報告に記載があっても、UI改修後に再確認していない項目はここに置きます。**「前回通ったから今回も通る」とは扱っていません。**

- **ネイティブウィンドウ上での手動確認は、上の表の範囲で実施しました。** 未確認なのは、AI接続を伴う画面（Live Illustratorの解説生成・停止、Studioの送信・ツール呼び出しの承認、文字起こし、MCP）です。**これらはローカルのLLMエンドポイントが要るため、この検査では動かしていません。**AI呼び出しの経路は `cargo test` のモックサーバ経由の統合テストで確認しています（Studioの `write_file` / `run_command` を含む、6回目の指摘の節を参照）。実LLMエンドポイントに対して、モデルが実際に `write_file` / `run_command` を狙いどおり呼び分けるかどうかは、接続先モデルの関数呼び出し精度に依存するため未確認です。
- 文字起こし（whisper）とMCPサーバ接続は今回変更しておらず、実機での再確認もしていません。
- `window.confirm` を使う2か所（保存場所の移動、ファイル変換の上書き確認）が、Tauriのwebviewで期待どおり表示されるかは未確認です。プロジェクト削除の `window.prompt` は今回アプリ内ダイアログへ置き換えたため、この経路は残っていません。
- VoiceOverでの読み上げ順は未確認です。
- 実AI／MCPサーバー、Whisper実モデル、実ファイル取り込みの統合テスト3件は今回実行していません（`cargo test` では無視扱い）。前回の実行結果は前回時点のコードに対するものです。
- Developer ID署名・公証は未実施のため、Gatekeeperは想定どおり拒否します。
- Windows／Linuxの実機検証は対象外です。

## セキュリティとデータ保全

v0.1.0では上記の通りRust側にも変更がありますが、いずれもサイズ上限・完了保証の追加とエラー処理の是正のみで、新しい通信経路や権限の追加はありません。前回検証した以下の性質は維持されています。

- APIキーはOS資格情報ストアへ保存し、WebViewやSQLite設定へ返しません。
- CSPは外部スクリプト、外部接続、埋め込みオブジェクトを既定拒否します。同梱書体は `font-src 'self'` の範囲内で、外部からの取得は発生しません。
- ユーザー指定のAI／MCP／URL取り込み／モデル取得以外にテレメトリ通信はありません。
- ZIPは展開前後の上限、閉じた相対経路、UUID、必須SHA-256を検査します。
- プロジェクトDBはSQLiteオンラインバックアップで取得し、使用中のWAL変更を保持します。

## 依存関係

- npm audit: 既知脆弱性0件（今回実行して確認）
- 追加した依存は `@fontsource-variable/geist` `@fontsource-variable/newsreader` `@fontsource-variable/jetbrains-mono` の3件のみで、いずれもOFL-1.1、ビルド時にwoff2へ解決される静的アセットです。実行時コードは増えていません。帰属表示と全文は `THIRD_PARTY_LICENSES.md` にあります。
- RustSecは脆弱性0件ですが、推移的依存に保守終了／健全性警告が残ります。大半はTauriのLinux向けGTK3依存で、macOS DMGには組み込まれません。上流更新を継続監視します。

## 公開前に人が行う確認

- **ビルドしたアプリを実際に起動し、主要画面を手で操作してください。** 今回の改修はUI全域に及ぶため、ここが最も重要な残作業です。
- 実際に配布するMacへDMGからインストールし、Gatekeeper表示と初回起動を確認
- VoiceOverで主要画面、ファイル選択、承認ダイアログの読み上げ順を確認
- 利用予定の実AI／MCPサーバーで接続テストと回答品質を確認
- Developer ID署名・公証を行う場合は、資格情報を設定した別ビルドで再度署名検証
