# WAKARU UI・LAN API・MCP 精査修正手順書

作成日: 2026-09-02

## 目的

1. 画面の右半分・下半分が欠ける原因を全体から洗い出し、一般的な横長・縦長・狭幅・低い高さ・高倍率表示で、主要操作と情報へ常に到達できるようにする。
2. ローカルネットワーク上のOpenAI互換・Anthropic互換APIへ、安全かつ設定どおり接続できることを静的に確認し、不備を修正する。
3. MCP stdioクライアントを現行仕様と照合し、初期化、能力交渉、ツール列挙・実行、通知、キャンセル、終了、エラー分離、安全境界を確認・修正する。

## 作業規律

- 既存データ、公開方針、未関連の変更を維持する。
- APIキー、MCP環境変数、資料本文をログ・テスト出力・リポジトリへ残さない。
- UI修正は単一画面の場当たり対応にせず、共通シェル、flex/gridの縮小規則、スクロール所有権、ダイアログ寸法、固定要素を横断して直す。
- 仕様上未対応の機能は、対応しているように見せず明示的に縮退または拒否する。
- 修正ごとに最小の回帰テストを追加し、最後に全ゲートを再実行する。

## 手順

### 1. 基準状態の固定

- Git状態、現行コミット、既存テスト、ビルド状態を記録する。
- 主要画面とオーバーレイを列挙し、レイアウト責任を持つCSS・Reactコンポーネントを対応付ける。
- 既存の静的監査・UI仕様・AI仕様・MCP仕様を読む。

### 2. UI静的監査

- `width` / `height` / `min-width` / `min-height` / `overflow` / `position: fixed|absolute` / viewport単位を全検索する。
- flex/grid子要素の`min-width: 0`・`min-height: 0`不足、固定列幅、固定フッター、二重スクロール、画面外ダイアログを検出する。
- ホーム、プロジェクト、Viewer、Studio、Live Illustrator、検索、設定、File Modifier、初回ウィザード、確認ダイアログを確認する。

### 3. UI実表示再現

- 開発版をローカルで起動し、少なくとも次を確認する。
  - 1440×900、1280×720、1024×768
  - 844×390、390×844
  - 720×1024、1024×480、480×1024
- 各寸法で横方向の意図しない欠落、下端の到達不能、ボタン・入力欄・モーダルの切断を確認する。
- 修正後に同じ寸法を再検証し、必要な箇所はスクリーンショットで目視確認する。

### 4. レスポンシブ修正

- アプリシェルを`100dvh`基準にし、スクロールする領域を明確にする。
- 主領域とサイドバーは利用可能幅に応じて縮小・折返し・縦積みを行う。
- 高さが低い画面では固定ヘッダー／フッターが本文を隠さないようにする。
- ダイアログ、ドロワー、ポップオーバーは`max-inline-size`・`max-block-size`と内部スクロールを持たせる。
- 主要文字、操作、フォーカス、タッチ対象を維持する。

### 5. LAN API静的監査

- Base URL正規化、`http://` LAN URL許可、末尾スラッシュ、`/v1`、認証ヘッダー、追加ヘッダー、タイムアウトを確認する。
- `/models`が無い場合の実チャット確認、OpenAI SSE、Anthropic SSE、`finish_reason`、途中切断、出力上限、ツール呼び出し、埋め込み未提供時のFTS縮退を確認する。
- SSRF防止対象である資料URL取り込みと、利用者が明示設定するAI接続先を混同していないことを確認する。
- 秘密情報がDB・設定・ログ・エラー本文へ漏れないことを確認する。

### 6. MCP仕様監査

- 公式MCP仕様の現行版を一次情報として確認する。
- stdioフレーミング、JSON-RPC 2.0、`initialize`、`notifications/initialized`、能力・protocolVersion交渉を確認する。
- `tools/list`のページング、`tools/call`のcontent・structuredContent・isError、通知、未知メッセージ、サーバー終了を確認する。
- stderr分離、プロセス監視、同時要求ID、タイムアウト、キャンセル、切断、再接続、環境変数・キーチェーン、安全な実行を確認する。
- HTTP transportはこの版で未対応なら、設定・コード・文書が一貫して明示拒否することを確認する。

### 7. 検証ゲート

- Frontend: typecheck、lint、Vitest、contrast、i18n、production build。
- Backend: fmt、Clippy、全Rustテスト、cargo-deny。
- UI: 全指定寸法で主要画面・モーダル・ドロワーを再確認。
- API/MCP: モック相互運用テスト、異常系、秘密非漏えいを確認。
- `git diff --check`と生成型定義の差分を確認する。

### 8. 完了記録

- 発見事項、修正、未対応範囲、検証結果を品質報告・引継ぎ文書へ反映する。
- 公開版へ反映する場合は、ソース非公開方針を維持し、配布物・公開文書・保守記録を同時更新する。

## 完了条件

- 指定した全寸法で主要情報と操作が画面内または適切なスクロールで到達可能。
- 右端・下端の意図しないクリップ、画面外固定要素、操作不能なモーダルがない。
- LAN内HTTP APIが明示設定として受理され、OpenAI/Anthropicの各経路が安全に組み立てられる。
- MCP stdioの初期化からツール実行・切断まで仕様準拠のテストが通る。
- 全自動ゲートが成功し、新規の重大・高優先度不具合が残っていない。

## 実施結果（2026-09-02）

- UI: 6種類のviewportと150%表示を実ブラウザで検証し、全主要画面の横欠けを解消。
- LAN API: `http://`を含む互換URLの扱いを維持し、URL・ヘッダー・timeout・能力プローブを強化。
- MCP: 2026-07-28 discover優先、旧初期化へのfallback、timeout、一覧更新、結果保持、秘密値マスキングを実装。
- 相互運用: 公式everything stdioサーバーの一覧取得と`echo`呼び出しに成功。
- 回帰: frontend全ゲート、Rust 185テスト、clippy、cargo-denyを通過。
