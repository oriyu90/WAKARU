守って欲しいこととやって欲しいこと。
WAKARUの開発が終わったら(全フェーズクリア、ユーザーに動作確認を最後依頼してOKだったら)、WAKARUというパブリックリポジトリを作成して、そこにReadme.mdとLicence.md(MIT licence)をGithubリポジトリにアップロードする。ソースコードはあげない。

そしてReleaseをpre releaseとしてv0.99として作成し、公開する。